-- =============================================================================
-- 블로그 "🙏 부탁해요~" 게시판(boardType: 'request') 지원
-- - 게시글 작성 시 "비밀기능" 체크 여부를 저장하는 컬럼 추가
--   (체크 시: 제목은 공개, 본문은 작성자/관리자만 열람 가능)
-- =============================================================================

ALTER TABLE blog_posts
  ADD COLUMN IF NOT EXISTS is_private_request boolean DEFAULT false;
